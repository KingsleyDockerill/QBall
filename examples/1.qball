((An example QBall program.
This will create a function(outarg) that will output 
a single argument.))

((The _ is equivelent to def in Python))
_outarg a;
  out a; ((Builtin for printing info))
end $ This can all be done on one line, but for readibility it will be on multiple

$ Support for floating points, strings, ints, and lists
outarg "Hello, world!"
outarg 5
outarg 5.5
outarg ["Hello, world!", "Hello, again!"]
