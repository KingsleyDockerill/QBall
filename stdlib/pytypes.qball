*dictionary keys values;
  $ Both keys and values are lists
  global keys = keys;
  global values = values;
_dictionary:get key;
  $ Get the key key's value and return it
  local c;
  for i in keys;
    if i == key;
      break;
    end
    c += 1;
  end
  return values[c];
end